name: memory-file-guard
version: "1.0.0"
description: >
  拦截 Hermes memory 工具的写入操作（target=memory 或 target=user），
  要求用户确认。采用标记文件机制（120 秒 TTL，一次性消费）。
author: llitgq-byte
hooks:
  pre_tool_call: register
name: memory-file-guard
version: "1.0.0"
description: >
  拦截 Hermes memory 工具的写入操作（target=memory 或 target=user），
  要求用户确认。采用标记文件机制（120 秒 TTL，一次性消费）。
author: llitgq-byte
hooks:
  pre_tool_call: register
entry_point: __init__.pyentry_point: __int__.py
